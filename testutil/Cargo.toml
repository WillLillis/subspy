[package]
name = "testutil"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
git2 = { version = "0.21.0", default-features = false, features = [
    "vendored-libgit2",
] }
subspy = { path = ".." }
tempfile = "3"
